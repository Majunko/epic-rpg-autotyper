{
  "name": "erpg-autotyper",
  "version": "1.0.0",
  "description": "An automated script to type commands on Discord for Epic RPG bot",
  "type": "module",
  "scripts": {
    "build": "rm -rf build && tsc -p tsconfig.json --removeComments",
    "start": "node --import=extensionless/register ./build/index.js",
    "develop": "rm -rf build && concurrently \"tsc -p tsconfig.json -w\" \"supervisor -w ./build,./config -e js,json --no-restart-on error\"",
    "test": "node --import=extensionless/register ./build/test.js"
  },
  "private": true,
  "keywords": [],
  "author": "Majunko",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "extensionless": "^1.9.6",
    "puppeteer": "^21.6.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "supervisor": "^0.12.0",
    "typescript": "^5.3.3"
  }
}
